export default { name:'promoteall', category:'owner', aliases:['makealladmins'], ownerOnly:true, async execute(sock,msg){ const c=msg.key.remoteJid; if(!c.endsWith('@g.us')) return; try{ const m=await sock.groupMetadata(c); const members=m.participants.filter(p=>!p.admin); for(const p of members){ try{ await sock.groupParticipantsUpdate(c,[p.id],'promote'); await new Promise(r=>setTimeout(r,300)); }catch(e){} } await sock.sendMessage(c,{text:'🦇 *ALL ASCENDED!*\nEvery soul is now an Alpha!\n\n> *Vampire Tech* 🧛'}); }catch(e){ await sock.sendMessage(c,{text:'💀 Need Alpha powers!'},{quoted:msg}); } } };
