export default { name:'blocklist', category:'owner', aliases:['blocked','listblocked'], ownerOnly:true, async execute(sock,msg){ const c=msg.key.remoteJid; try{ const list=await sock.fetchBlocklist(); await sock.sendMessage(c,{text:list.length?`🚫 *BLOCKED SOULS (${list.length})*\n\n${list.map((j,i)=>`${i+1}. ${j.split('@')[0]}`).join('\n')}\n\n> *Vampire Tech* 🧛`:'📭 *No blocked souls!*\n\n> *Vampire Tech* 🧛`},{quoted:msg}); }catch(e){ await sock.sendMessage(c,{text:'💀 Failed to fetch!'},{quoted:msg}); } } };
