export default { name: 'gclist', description: 'List all groups bot is in', category: 'owner', ownerOnly: true, async execute(sock, msg, args) { const chatId=msg.key.remoteJid; try{ const groups=await sock.groupFetchAllParticipating(); const list=Object.values(groups); let text=`📋 *Groups (${list.length})*\n\n`; list.forEach((g,i)=>{ text+=`${i+1}. ${g.subject}\n   👥 ${g.participants?.length||0} members\n\n`; }); await sock.sendMessage(chatId,{text:text+'> *Powered by Vampire Tech*'},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
