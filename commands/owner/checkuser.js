export default { name: 'checkuser', description: 'Check if number is on WhatsApp', category: 'owner', ownerOnly: true, async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const number=args[0]?.replace(/[^0-9]/g,''); if(!number) return sock.sendMessage(chatId,{text:'❌ .checkuser <number>'},{quoted:msg}); try{ const [result]=await sock.onWhatsApp(number+'@s.whatsapp.net'); await sock.sendMessage(chatId,{text:`🔍 *Check User*\n📞 +${number}\n${result?.exists?'✅ On WhatsApp':'❌ Not on WhatsApp'}\n\n> *Powered by Vampire Tech*`},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
