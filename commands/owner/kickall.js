export default { name:'kickall', category:'owner', aliases:['removeall','purge'], ownerOnly:true, async execute(sock,msg){ const c=msg.key.remoteJid; if(!c.endsWith('@g.us')) return; try{ const m=await sock.groupMetadata(c); const members=m.participants.filter(p=>p.id!==sock.user.id&&!p.admin); await sock.sendMessage(c,{text:`💀 *PURGING ${members.length} souls...*`}); for(const p of members){ try{ await sock.groupParticipantsUpdate(c,[p.id],'remove'); await new Promise(r=>setTimeout(r,500)); }catch(e){} } await sock.sendMessage(c,{text:'💀 *COVEN PURGED!*\nAll mortals removed.\n\n> *Vampire Tech* 🧛'}); }catch(e){ await sock.sendMessage(c,{text:'💀 Need Alpha powers!'},{quoted:msg}); } } };
