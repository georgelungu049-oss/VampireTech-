export default { name: 'self', description: 'Send message to yourself', category: 'owner', ownerOnly: true, async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const message=args.join(' '); if(!message) return sock.sendMessage(chatId,{text:'❌ .self <message>'},{quoted:msg}); await sock.sendMessage(chatId,{text:`📝 *Note to self:* ${message}\n\n> *Powered by Vampire Tech*`},{quoted:msg}); } };
