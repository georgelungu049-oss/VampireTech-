export default { name:'demoteall', category:'owner', aliases:['removealladmins'], ownerOnly:true, async execute(sock,msg){ const c=msg.key.remoteJid; if(!c.endsWith('@g.us')) return; try{ const m=await sock.groupMetadata(c); const admins=m.participants.filter(p=>p.admin&&p.id!==sock.user.id); for(const a of admins){ try{ await sock.groupParticipantsUpdate(c,[a.id],'demote'); await new Promise(r=>setTimeout(r,300)); }catch(e){} } await sock.sendMessage(c,{text:'💀 *ALL ALPHAS FALLEN!*\nOnly the Vampire Lord remains.\n\n> *Vampire Tech* 🧛'}); }catch(e){ await sock.sendMessage(c,{text:'💀 Need super Alpha powers!'},{quoted:msg}); } } };
