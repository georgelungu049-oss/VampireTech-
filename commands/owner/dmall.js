export default { name: 'dmall', description: 'DM all group members', category: 'owner', ownerOnly: true, async execute(sock, msg, args) { const chatId = msg.key.remoteJid; if (!chatId.endsWith('@g.us')) return sock.sendMessage(chatId, { text: '❌ Group only!' }, { quoted: msg }); const message = args.join(' ') || 'Hello!'; const meta = await sock.groupMetadata(chatId); await sock.sendMessage(chatId, { text: `📨 Sending DM to ${meta.participants.length} members...` }, { quoted: msg }); for (const p of meta.participants) { try { await sock.sendMessage(p.id, { text: message }); await new Promise(r => setTimeout(r, 1000)); } catch (e) {} } await sock.sendMessage(chatId, { text: '✅ DMs sent!' }, { quoted: msg }); } };
