export default { name: 'dog', description: 'Random dog picture', category: 'utility', aliases: ['doggo','puppy'], async execute(sock, msg, args) { const chatId = msg.key.remoteJid; try { const res = await fetch('https://dog.ceo/api/breeds/image/random'); const data = await res.json(); await sock.sendMessage(chatId, { image: { url: data.message }, caption: '🐕 *Woof!*\n\n> *Powered by Vampire Tech*' }, { quoted: msg }); } catch (e) { await sock.sendMessage(chatId, { text: '❌ Failed!' }, { quoted: msg }); } } };
