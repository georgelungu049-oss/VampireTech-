export default { name:'age', description:'Calculate age', category:'utility', aliases:['birth'], async execute(sock,msg,args){ const c=msg.key.remoteJid; const y=parseInt(args[0]); if(!y) return sock.sendMessage(c,{text:'❌ .age <birth year>'},{quoted:msg}); await sock.sendMessage(c,{text:`🎂 Born ${y} = ${new Date().getFullYear()-y} years old\n\n> *Vampire Tech*`},{quoted:msg}); } };
