export default { name: 'cat', description: 'Random cat picture', category: 'utility', aliases: ['kitty','meow'], async execute(sock, msg, args) { const chatId = msg.key.remoteJid; try { const res = await fetch('https://api.thecatapi.com/v1/images/search'); const data = await res.json(); await sock.sendMessage(chatId, { image: { url: data[0].url }, caption: '🐱 *Meow!*\n\n⚡ *Powered by Vampire Tech* 🧛' }, { quoted: msg }); } catch (e) { await sock.sendMessage(chatId, { text: '❌ Failed!' }, { quoted: msg }); } } };
