export default { name:'sticker', category:'utility', aliases:['st','s'], async execute(sock,msg){ const c=msg.key.remoteJid; const q=msg.message?.extendedTextMessage?.contextInfo?.quotedMessage; if(!q?.imageMessage&&!q?.videoMessage) return sock.sendMessage(c,{text:'🦇 *Reply to an image/video to create a sticker!*'},{quoted:msg}); try{ const {downloadMediaMessage}=await import('@whiskeysockets/baileys'); const b=await downloadMediaMessage({message:q},'buffer',{},{logger:{level:'silent'}}); await sock.sendMessage(c,{sticker:b},{quoted:msg}); }catch(e){ await sock.sendMessage(c,{text:'💀 Failed to create sticker!'},{quoted:msg}); } } };
