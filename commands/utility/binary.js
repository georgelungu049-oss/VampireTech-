export default { name:'binary', category:'utility', aliases:['bin'], async execute(sock,msg,args){ const c=msg.key.remoteJid; const t=args.join(' '); if(!t) return sock.sendMessage(c,{text:'🦇 .binary <text>'},{quoted:msg}); await sock.sendMessage(c,{text:`🔢 ${t.split('').map(c=>c.charCodeAt(0).toString(2).padStart(8,'0')).join(' ').substring(0,500)}\n\n> *Vampire Tech*`},{quoted:msg}); } };
