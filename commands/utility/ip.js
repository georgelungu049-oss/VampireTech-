export default { name: 'ip', description: 'Look up IP info', category: 'utility', aliases: ['iplookup'], async execute(sock, msg, args) { const chatId = msg.key.remoteJid; const ip = args[0] || '8.8.8.8'; try { const res = await fetch(`http://ip-api.com/json/${ip}`); const data = await res.json(); await sock.sendMessage(chatId, { text: `🌐 *IP Lookup*\n📡 ${data.query}\n📍 ${data.city}, ${data.country}\n🏢 ${data.isp}\n\n⚡ *Powered by Vampire Tech* 🧛` }, { quoted: msg }); } catch (e) { await sock.sendMessage(chatId, { text: '❌ Failed!' }, { quoted: msg }); } } };
