export default { name:'pick', category:'games', aliases:['choose','random'], async execute(sock,msg,args){ const c=msg.key.remoteJid; const o=args.join(' ').split(',').map(s=>s.trim()).filter(s=>s); if(o.length<2) return sock.sendMessage(c,{text:'🦇 .pick option1, option2, option3'},{quoted:msg}); await sock.sendMessage(c,{text:`🎯 *I Choose:* ${o[Math.floor(Math.random()*o.length)]}\n\n> *Vampire Tech*`},{quoted:msg}); } };
