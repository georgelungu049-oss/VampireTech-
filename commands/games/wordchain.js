export default { name: 'wordchain', description: 'Word chain game', category: 'games', aliases: ['chain'], async execute(sock, msg, args) { const chatId = msg.key.remoteJid; const word = args[0]?.toLowerCase()||'vampire'; const last = word[word.length-1]; await sock.sendMessage(chatId, { text: `🔗 *Word Chain*\nYour word: ${word}\nNext must start with: *${last.toUpperCase()}*\n\nExample: .chain ${last}...\n\n⚡ *Powered by Vampire Tech* 🧛` }, { quoted: msg }); } };
