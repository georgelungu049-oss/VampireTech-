export default { name: 'github', description: 'Search GitHub repos', category: 'search', aliases: ['gh','gitsearch'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const q=args.join(' '); if(!q) return sock.sendMessage(chatId,{text:'❌ .github <query>'},{quoted:msg}); try{ const r=await fetch(`https://apis.xwolf.space/api/search/github?q=${encodeURIComponent(q)}`); const d=await r.json(); const repos=(d.result||[]).slice(0,5).map((r,i)=>`${i+1}. ${r.name||r.full_name}\n⭐ ${r.stars||0}`).join('\n\n'); await sock.sendMessage(chatId,{text:`📂 *GitHub*\n${repos}\n\n⚡ *Vampire Tech* 🧛`},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
