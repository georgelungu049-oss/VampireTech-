export default { name: 'emoji', description: 'Search emojis', category: 'search', aliases: ['findemoji','emojisearch'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const q=args.join(' ')||'fire'; try{ const r=await fetch(`https://apis.xwolf.space/api/search/emoji?q=${encodeURIComponent(q)}`); const d=await r.json(); const emojis=(d.result||[]).slice(0,10).map(e=>e.emoji||e).join(' '); await sock.sendMessage(chatId,{text:`🔍 *Emoji: ${q}*\n${emojis}\n\n⚡ *Vampire Tech* 🧛`},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
