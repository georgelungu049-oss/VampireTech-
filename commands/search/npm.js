export default { name: 'npm', description: 'Search NPM packages', category: 'search', aliases: ['npmsearch'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const q=args.join(' '); if(!q) return sock.sendMessage(chatId,{text:'❌ .npm <package>'},{quoted:msg}); try{ const r=await fetch(`https://apis.xwolf.space/api/search/npm?q=${encodeURIComponent(q)}`); const d=await r.json(); const pkgs=(d.result||[]).slice(0,5).map((p,i)=>`${i+1}. ${p.name} v${p.version}`).join('\n'); await sock.sendMessage(chatId,{text:`📦 *NPM: ${q}*\n${pkgs}\n\n⚡ *Vampire Tech* 🧛`},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
