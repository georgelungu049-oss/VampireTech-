export default { name: 'hack', description: 'Fake hack someone', category: 'fun', aliases: ['hacker'], async execute(sock, msg, args) { const chatId = msg.key.remoteJid; const target = args[0] || 'Installing virus...'; const sent = await sock.sendMessage(chatId, { text: `💻 *HACKING ${target}*\n0% [░░░░░░░░░░]` }); for (let i=10;i<=100;i+=10){ await new Promise(r=>setTimeout(r,500)); await sock.sendMessage(chatId, { text: `💻 *HACKING ${target}*\n${i}% [${'█'.repeat(i/10)}${'░'.repeat(10-i/10)}]`, edit: sent.key }); } await sock.sendMessage(chatId, { text: `✅ *HACKED SUCCESSFULLY!*\n😂 Just kidding!\n\n> *Powered by Vampire Tech*` }, { quoted: msg }); } };
