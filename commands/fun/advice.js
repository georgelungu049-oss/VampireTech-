export default { name:'advice', description:'Random advice', category:'fun', async execute(sock,msg){ const c=msg.key.remoteJid; try{ const r=await fetch('https://api.adviceslip.com/advice'); const d=await r.json(); await sock.sendMessage(c,{text:`💡 ${d.slip.advice}\n\n> *Vampire Tech*`},{quoted:msg}); }catch(e){ await sock.sendMessage(c,{text:'💡 Never give up!\n\n> *Vampire Tech*'},{quoted:msg}); } } };
