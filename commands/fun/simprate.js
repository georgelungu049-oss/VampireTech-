export default { name: 'simprate', description: 'Rate simp level', category: 'fun', aliases: ['simp'], async execute(sock, msg, args) { const chatId = msg.key.remoteJid; const mentioned = msg.message?.extendedTextMessage?.contextInfo?.mentionedJid?.[0]; const name = mentioned ? `@${mentioned.split('@')[0]}` : (args[0] || 'You'); const rate = Math.floor(Math.random() * 100); await sock.sendMessage(chatId, { text: `💘 *Simp Rate*\n${name}: ${rate}% simp\n\n> *Powered by Vampire Tech*`, mentions: mentioned?[mentioned]:[] }, { quoted: msg }); } };
