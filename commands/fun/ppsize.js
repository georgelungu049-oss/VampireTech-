export default { name: 'ppsize', description: 'Check PP size 😂', category: 'fun', async execute(sock, msg, args) { const chatId = msg.key.remoteJid; const mentioned = msg.message?.extendedTextMessage?.contextInfo?.mentionedJid?.[0]; const name = mentioned ? `@${mentioned.split('@')[0]}` : (args[0] || 'You'); const size = Math.floor(Math.random() * 30) + 1; const bar = '='.repeat(size); await sock.sendMessage(chatId, { text: `📏 *PP Size*\n${name}: ${bar}> ${size}cm\n\n⚡ *Powered by Vampire Tech* 🧛`, mentions: mentioned?[mentioned]:[] }, { quoted: msg }); } };
