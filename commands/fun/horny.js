export default { name: 'horny', description: 'Horny test 😳', category: 'fun', async execute(sock, msg, args) { const chatId = msg.key.remoteJid; const mentioned = msg.message?.extendedTextMessage?.contextInfo?.mentionedJid?.[0]; const name = mentioned ? `@${mentioned.split('@')[0]}` : (args[0] || 'You'); const pct = Math.floor(Math.random()*100); await sock.sendMessage(chatId, { text: `😳 *Horny Test*\n${name}: ${pct}% horny\n${pct>80?'🥵 TOO HORNY!':pct>50?'😏 Kinda horny':pct>20?'🙂 Normal':'😇 Pure soul'}\n\n> *Powered by Vampire Tech*`, mentions: mentioned?[mentioned]:[] }, { quoted: msg }); } };
