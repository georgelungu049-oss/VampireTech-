export default { name: 'gaytest', description: 'How gay are you? 🌈', category: 'fun', aliases: ['gay'], async execute(sock, msg, args) { const chatId = msg.key.remoteJid; const mentioned = msg.message?.extendedTextMessage?.contextInfo?.mentionedJid?.[0]; const name = mentioned ? `@${mentioned.split('@')[0]}` : (args[0] || 'You'); const percent = Math.floor(Math.random() * 100); await sock.sendMessage(chatId, { text: `🌈 *Gay Test*\n${name}: ${percent}% gay\n\n> *Powered by Vampire Tech*`, mentions: mentioned?[mentioned]:[] }, { quoted: msg }); } };
