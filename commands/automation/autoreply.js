export default { name: 'autoreply', description: 'Set auto reply message', category: 'automation', aliases: ['automsg'], async execute(sock, msg, args) { const chatId = msg.key.remoteJid; const reply = args.join(' ') || 'I am currently away. Will reply soon!'; await sock.sendMessage(chatId, { text: `✅ Auto-reply set to: "${reply}"\n\n> *Powered by Vampire Tech*` }, { quoted: msg }); } };
