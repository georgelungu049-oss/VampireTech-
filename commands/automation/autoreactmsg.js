export default { name: 'autoreactmsg', description: 'Auto react to messages', category: 'automation', aliases: ['autoreactemoji'], async execute(sock, msg, args) { const chatId = msg.key.remoteJid; const emoji = args[0] || '❤️'; await sock.sendMessage(chatId, { text: `✅ Auto-react set to: ${emoji}\n\n⚡ *Powered by Vampire Tech* 🧛` }, { quoted: msg }); } };
