export default { name: 'anticall', description: 'Auto reject calls', category: 'automation', async execute(sock, msg, args) { const chatId = msg.key.remoteJid; const action = args[0]?.toLowerCase() || 'on'; await sock.sendMessage(chatId, { text: action === 'on' ? '📵 Anti-Call ENABLED! All calls will be rejected.\n\n> *Powered by Vampire Tech*' : '📵 Anti-Call DISABLED!\n\n> *Powered by Vampire Tech*' }, { quoted: msg }); } };
