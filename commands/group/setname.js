export default { name:'setname', category:'group', aliases:['groupname','covenname'], async execute(sock,msg,args){ const c=msg.key.remoteJid; if(!c.endsWith('@g.us')) return; const n=args.join(' '); if(!n) return sock.sendMessage(c,{text:'🦇 *What shall this coven be called?*'},{quoted:msg}); try{ await sock.groupUpdateSubject(c,n); await sock.sendMessage(c,{text:`🦇 *COVEN RENAMED!*\nNow known as: *${n}*\n\n> *Vampire Tech* 🧛`},{quoted:msg}); }catch(e){ await sock.sendMessage(c,{text:'💀 Need Alpha powers!'},{quoted:msg}); } } };
