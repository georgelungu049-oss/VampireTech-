export default { name: 'setname', description: 'Change group name', category: 'group', aliases: ['groupname', 'setgroup'], async execute(sock, msg, args) { const chatId = msg.key.remoteJid; if (!chatId.endsWith('@g.us')) return sock.sendMessage(chatId, { text: '❌ Group only!' }, { quoted: msg }); const name = args.join(' '); if (!name) return sock.sendMessage(chatId, { text: '❌ Provide a name!' }, { quoted: msg }); try { await sock.groupUpdateSubject(chatId, name); await sock.sendMessage(chatId, { text: `✅ Group renamed to: *${name}*\n\n⚡ *Powered by Vampire Tech* 🧛` }, { quoted: msg }); } catch (e) { await sock.sendMessage(chatId, { text: '❌ Failed!' }, { quoted: msg }); } } };
