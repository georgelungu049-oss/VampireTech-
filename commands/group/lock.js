export default { name: 'lock', description: 'Lock group settings', category: 'group', async execute(sock, msg, args) { const chatId = msg.key.remoteJid; if (!chatId.endsWith('@g.us')) return sock.sendMessage(chatId, { text: '❌ Group only!' }, { quoted: msg }); const setting = args[0]?.toLowerCase() || 'all'; try { if (setting === 'all' || setting === 'info') await sock.groupSettingUpdate(chatId, 'locked'); await sock.sendMessage(chatId, { text: `🔒 Group ${setting} locked!\n\n> *Powered by Vampire Tech*` }, { quoted: msg }); } catch (e) { await sock.sendMessage(chatId, { text: '❌ Failed! Need admin.' }, { quoted: msg }); } } };
