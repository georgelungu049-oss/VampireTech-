export default { name: 'desc', description: 'Show/set group description', category: 'group', aliases: ['description','about'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; if(!chatId.endsWith('@g.us')) return sock.sendMessage(chatId,{text:'❌ Group only!'},{quoted:msg}); try{ const meta=await sock.groupMetadata(chatId); await sock.sendMessage(chatId,{text:`📝 *Description*\n${meta.desc||'No description'}\n\n> *Powered by Vampire Tech*`},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
