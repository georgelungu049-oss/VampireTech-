export default { name:'groupinfo', category:'group', aliases:['coveninfo','gcinfo'], async execute(sock,msg){ const c=msg.key.remoteJid; if(!c.endsWith('@g.us')) return; try{ const m=await sock.groupMetadata(c); const admins=m.participants.filter(p=>p.admin).length; await sock.sendMessage(c,{text:`🦇 *COVEN INFO*\n\n📛 Name: ${m.subject}\n👥 Souls: ${m.participants.length}\n👑 Alphas: ${admins}\n📅 Created: ${new Date(m.creation*1000).toLocaleDateString()}\n🔒 Mode: ${m.restrict?'Restricted':'Open'}\n\n> *Vampire Tech* 🧛`},{quoted:msg}); }catch(e){} } };
