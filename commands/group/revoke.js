export default { name: 'revoke', description: 'Revoke group invite link', category: 'group', aliases: ['resetlink'], async execute(sock, msg, args) { const chatId = msg.key.remoteJid; if (!chatId.endsWith('@g.us')) return sock.sendMessage(chatId, { text: '❌ Group only!' }, { quoted: msg }); await sock.groupRevokeInvite(chatId); await sock.sendMessage(chatId, { text: '🔗 Group link revoked!\n\n> *Powered by Vampire Tech*' }, { quoted: msg }); } };
