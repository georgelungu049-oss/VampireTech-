export default { name: 'antispam', description: 'Prevent spam in group', category: 'group', async execute(sock, msg, args) { const chatId = msg.key.remoteJid; if (!chatId.endsWith('@g.us')) return sock.sendMessage(chatId, { text: '❌ Group only!' }, { quoted: msg }); await sock.sendMessage(chatId, { text: '🛡️ Anti-Spam ENABLED! Spammers will be removed.\n\n⚡ *Powered by Vampire Tech* 🧛' }, { quoted: msg }); } };
