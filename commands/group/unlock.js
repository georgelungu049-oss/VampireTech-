export default { name: 'unlock', description: 'Unlock group settings', category: 'group', async execute(sock, msg, args) { const chatId = msg.key.remoteJid; if (!chatId.endsWith('@g.us')) return sock.sendMessage(chatId, { text: '❌ Group only!' }, { quoted: msg }); try { await sock.groupSettingUpdate(chatId, 'unlocked'); await sock.sendMessage(chatId, { text: '🔓 Group unlocked!\n\n> *Powered by Vampire Tech*' }, { quoted: msg }); } catch (e) { await sock.sendMessage(chatId, { text: '❌ Failed! Need admin.' }, { quoted: msg }); } } };
