export default { name: 'poll', description: 'Create a poll', category: 'group', async execute(sock, msg, args) { const chatId = msg.key.remoteJid; const text = args.join(' '); if (!text.includes('|')) return sock.sendMessage(chatId, { text: '❌ Use: .poll Question? | Option1 | Option2 | Option3' }, { quoted: msg }); const [q, ...opts] = text.split('|').map(s => s.trim()); await sock.sendMessage(chatId, { poll: { name: q, values: opts, selectableCount: 1 } }, { quoted: msg }); } };
