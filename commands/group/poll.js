export default { name:'poll', category:'group', aliases:['vote','covenvote'], async execute(sock,msg,args){ const c=msg.key.remoteJid; const t=args.join(' '); if(!t.includes('|')) return sock.sendMessage(c,{text:'🦇 *Create a coven vote!*\n.poll Question? | Option1 | Option2'},{quoted:msg}); const[q,...o]=t.split('|').map(s=>s.trim()).filter(s=>s); if(o.length<2) return; await sock.sendMessage(c,{poll:{name:`🦇 ${q}`,values:o,selectableCount:1}}); } };
