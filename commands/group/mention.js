export default { name: 'mention', description: 'Mention specific members', category: 'group', aliases: ['tag','call'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; if(!chatId.endsWith('@g.us')) return sock.sendMessage(chatId,{text:'❌ Group only!'},{quoted:msg}); const mentioned=msg.message?.extendedTextMessage?.contextInfo?.mentionedJid; if(!mentioned?.length) return sock.sendMessage(chatId,{text:'❌ Mention someone!'},{quoted:msg}); await sock.sendMessage(chatId,{text:`📢 *You've been called!*\n${args.join(' ')||'Attention!'}`,mentions:mentioned},{quoted:msg}); } };
