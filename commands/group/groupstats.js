export default { name: 'groupstats', description: 'Show group statistics', category: 'group', aliases: ['gcstats','activity'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; if(!chatId.endsWith('@g.us')) return sock.sendMessage(chatId,{text:'❌ Group only!'},{quoted:msg}); try{ const meta=await sock.groupMetadata(chatId); const admins=meta.participants.filter(p=>p.admin).length; const members=meta.participants.length-admins; await sock.sendMessage(chatId,{text:`📊 *Group Stats*\n\n👥 Total: ${meta.participants.length}\n👑 Admins: ${admins}\n👤 Members: ${members}\n📅 Created: ${new Date(meta.creation*1000).toLocaleDateString()}\n\n> *Powered by Vampire Tech*`},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
