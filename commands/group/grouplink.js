export default { name: 'grouplink', description: 'Get group invite link', category: 'group', async execute(sock, msg, args) { const chatId = msg.key.remoteJid; if (!chatId.endsWith('@g.us')) return sock.sendMessage(chatId, { text: '❌ Group only!' }, { quoted: msg }); const code = await sock.groupInviteCode(chatId); await sock.sendMessage(chatId, { text: `🔗 https://chat.whatsapp.com/${code}\n\n⚡ *Powered by Vampire Tech* 🧛` }, { quoted: msg }); } };
