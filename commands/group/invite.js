export default { name:'invite', category:'group', aliases:['covenlink','gclink','link'], async execute(sock,msg){ const c=msg.key.remoteJid; if(!c.endsWith('@g.us')) return; try{ const code=await sock.groupInviteCode(c); await sock.sendMessage(c,{text:`🦇 *COVEN GATE*\n\nSummon more souls to the darkness:\nhttps://chat.whatsapp.com/${code}\n\n> *Vampire Tech* 🧛`},{quoted:msg}); }catch(e){} } };
