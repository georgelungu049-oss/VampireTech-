export default { name: 'creator', description: 'Show group creator', category: 'group', aliases: ['founder','owner'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; if(!chatId.endsWith('@g.us')) return sock.sendMessage(chatId,{text:'❌ Group only!'},{quoted:msg}); try{ const meta=await sock.groupMetadata(chatId); const owner=meta.owner||'Unknown'; await sock.sendMessage(chatId,{text:`👑 *Creator*\n@${owner.split('@')[0]}\n\n> *Powered by Vampire Tech*`,mentions:[owner]},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
