export default { name:'members', category:'group', aliases:['souls','soulcount'], async execute(sock,msg){ const c=msg.key.remoteJid; if(!c.endsWith('@g.us')) return; try{ const m=await sock.groupMetadata(c); await sock.sendMessage(c,{text:`🦇 *COVEN SOULS: ${m.participants.length}*\n\n👑 Alphas: ${m.participants.filter(p=>p.admin).length}\n👤 Mortals: ${m.participants.filter(p=>!p.admin).length}\n\n> *Vampire Tech* 🧛`},{quoted:msg}); }catch(e){} } };
