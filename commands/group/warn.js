export default { name: 'warn', description: 'Warn a member', category: 'group', aliases: ['warning'], async execute(sock, msg, args) { const chatId = msg.key.remoteJid; if (!chatId.endsWith('@g.us')) return sock.sendMessage(chatId, { text: '❌ Group only!' }, { quoted: msg }); const target = msg.message?.extendedTextMessage?.contextInfo?.mentionedJid?.[0]; if (!target) return sock.sendMessage(chatId, { text: '❌ Mention someone to warn!' }, { quoted: msg }); await sock.sendMessage(chatId, { text: `⚠️ *WARNING!*\n👤 @${target.split('@')[0]}\nReason: ${args.join(' ')||'No reason'}\n⚠️ 3 warnings = KICK\n\n> *Powered by Vampire Tech*`, mentions: [target] }, { quoted: msg }); } };
