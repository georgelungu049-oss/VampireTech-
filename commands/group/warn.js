export default { name: 'warn', description: 'Warn a member (3 warnings = kick)', category: 'group', async execute(sock, msg, args) { const chatId=msg.key.remoteJid; if(!chatId.endsWith('@g.us')) return sock.sendMessage(chatId,{text:'❌ Group only!'},{quoted:msg}); const mentioned=msg.message?.extendedTextMessage?.contextInfo?.mentionedJid?.[0]; if(!mentioned) return sock.sendMessage(chatId,{text:'❌ Mention someone!'},{quoted:msg}); const reason=args.join(' ')||'No reason'; await sock.sendMessage(chatId,{text:`⚠️ *WARNING!*\n👤 @${mentioned.split('@')[0]}\n📝 ${reason}\n\n⚠️ 3 warnings = KICK\n\n> *Powered by Vampire Tech*`,mentions:[mentioned]},{quoted:msg}); } };
