export default { name:'tagall', category:'group', aliases:['everyone','all','summon'], async execute(sock,msg,args){ const c=msg.key.remoteJid; if(!c.endsWith('@g.us')) return; try{ const m=await sock.groupMetadata(c); const mentions=m.participants.map(p=>p.id); const msg2=args.join(' ')||'The Vampire Lord summons you!'; await sock.sendMessage(c,{text:`🦇 *COVEN SUMMONS*\n\n${msg2}\n\n> *Vampire Tech* 🧛`,mentions},{quoted:msg}); }catch(e){} } };
