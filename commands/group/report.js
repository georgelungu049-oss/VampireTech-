export default { name: 'report', description: 'Report a user to admins', category: 'group', async execute(sock, msg, args) { const chatId = msg.key.remoteJid; const mentioned = msg.message?.extendedTextMessage?.contextInfo?.mentionedJid?.[0]; if (!mentioned) return sock.sendMessage(chatId, { text: '❌ Mention someone to report!' }, { quoted: msg }); const reason = args.join(' ') || 'No reason provided'; await sock.sendMessage(chatId, { text: `🚨 *REPORT*\n👤 @${mentioned.split('@')[0]}\n📝 ${reason}\n\nAdmins please take action!\n\n> *Powered by Vampire Tech*`, mentions: [mentioned] }, { quoted: msg }); } };
