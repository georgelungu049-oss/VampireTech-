export default { name: 'resetlink', description: 'Reset group invite link', category: 'group', aliases: ['revokelink','newlink'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; if(!chatId.endsWith('@g.us')) return sock.sendMessage(chatId,{text:'❌ Group only!'},{quoted:msg}); try{ await sock.groupRevokeInvite(chatId); const code=await sock.groupInviteCode(chatId); await sock.sendMessage(chatId,{text:`🔗 *Link Reset!*\nNew: https://chat.whatsapp.com/${code}\n\n> *Powered by Vampire Tech*`},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Need admin!'},{quoted:msg}); } } };
