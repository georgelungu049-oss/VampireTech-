export default { name: 'setdesc', description: 'Change group description', category: 'group', async execute(sock, msg, args) { const chatId = msg.key.remoteJid; if (!chatId.endsWith('@g.us')) return sock.sendMessage(chatId, { text: '❌ Group only!' }, { quoted: msg }); const desc = args.join(' '); if (!desc) return sock.sendMessage(chatId, { text: '❌ Provide a description!' }, { quoted: msg }); try { await sock.groupUpdateDescription(chatId, desc); await sock.sendMessage(chatId, { text: `✅ Description updated!\n\n> *Powered by Vampire Tech*` }, { quoted: msg }); } catch (e) { await sock.sendMessage(chatId, { text: '❌ Failed!' }, { quoted: msg }); } } };
