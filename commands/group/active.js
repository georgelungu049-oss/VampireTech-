export default { name: 'active', description: 'Show most active members', category: 'group', aliases: ['topmembers','activeusers'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; if(!chatId.endsWith('@g.us')) return sock.sendMessage(chatId,{text:'❌ Group only!'},{quoted:msg}); try{ const meta=await sock.groupMetadata(chatId); const members=meta.participants.slice(0,10); let text='🔥 *Active Members*\n\n'; members.forEach((p,i)=>{ text+=`${i+1}. @${p.id.split('@')[0]} ${p.admin?'👑':''}\n`; }); await sock.sendMessage(chatId,{text:text+'\n> *Powered by Vampire Tech*',mentions:members.map(p=>p.id)},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
