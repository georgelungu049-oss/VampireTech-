export default { name: 'admins', description: 'List all group admins', category: 'group', aliases: ['adminlist'], async execute(sock, msg, args) { const chatId = msg.key.remoteJid; if (!chatId.endsWith('@g.us')) return sock.sendMessage(chatId, { text: '❌ Group only!' }, { quoted: msg }); try { const meta = await sock.groupMetadata(chatId); const admins = meta.participants.filter(p => p.admin); let text = `👑 *ADMINS (${admins.length})*\n\n`; admins.forEach((p, i) => { text += `${i+1}. @${p.id.split('@')[0]} ${p.admin === 'superadmin' ? '⭐' : '🔰'}\n`; }); await sock.sendMessage(chatId, { text: text + '\n> *Powered by Vampire Tech*', mentions: admins.map(p => p.id) }, { quoted: msg }); } catch (e) { await sock.sendMessage(chatId, { text: '❌ Failed!' }, { quoted: msg }); } } };
