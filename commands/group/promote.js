export default { name: 'promote', description: 'Promote member to admin', category: 'group', aliases: ['makeadmin'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; if(!chatId.endsWith('@g.us')) return sock.sendMessage(chatId,{text:'❌ Group only!'},{quoted:msg}); const mentioned=msg.message?.extendedTextMessage?.contextInfo?.mentionedJid?.[0]; if(!mentioned) return sock.sendMessage(chatId,{text:'❌ Mention someone!'},{quoted:msg}); try{ await sock.groupParticipantsUpdate(chatId,[mentioned],'promote'); await sock.sendMessage(chatId,{text:`✅ Promoted @${mentioned.split('@')[0]} to admin!`,mentions:[mentioned]},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Need admin permissions!'},{quoted:msg}); } } };
