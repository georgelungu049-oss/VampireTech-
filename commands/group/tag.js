export default { name: 'tag', description: 'Tag specific members by mentioning them', category: 'group', aliases: ['taguser','mentionuser'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; if(!chatId.endsWith('@g.us')) return sock.sendMessage(chatId,{text:'❌ Group only!'},{quoted:msg}); const mentioned=msg.message?.extendedTextMessage?.contextInfo?.mentionedJid; if(!mentioned?.length) return sock.sendMessage(chatId,{text:'❌ Mention someone!'},{quoted:msg}); const text=args.join(' ')||'🔔 You have been tagged!'; await sock.sendMessage(chatId,{text,mentions:mentioned},{quoted:msg}); } };
