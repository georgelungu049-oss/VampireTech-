export default { name: 'hidetag', description: 'Tag all members without showing mention', category: 'group', aliases: ['htag','silenttag'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; if(!chatId.endsWith('@g.us')) return sock.sendMessage(chatId,{text:'❌ Group only!'},{quoted:msg}); try{ const meta=await sock.groupMetadata(chatId); const mentions=meta.participants.map(p=>p.id); const text=args.join(' ')||'👀'; await sock.sendMessage(chatId,{text,mentions},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
