export default { name: 'report', description: 'Report a bug to owner', category: 'extra', async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const bug=args.join(' '); if(!bug) return sock.sendMessage(chatId,{text:'❌ .report <bug description>'},{quoted:msg}); await sock.sendMessage(chatId,{text:'✅ Bug reported to owner!\n\n> *Powered by Vampire Tech*'},{quoted:msg}); } };
