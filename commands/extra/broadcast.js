export default { name: 'broadcast', description: 'Send broadcast message', category: 'extra', ownerOnly: true, async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const message=args.join(' '); if(!message) return sock.sendMessage(chatId,{text:'❌ .broadcast <message>'},{quoted:msg}); await sock.sendMessage(chatId,{text:'📢 Broadcast sent!\n\n> *Powered by Vampire Tech*'},{quoted:msg}); } };
