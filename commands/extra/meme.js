export default { name: 'meme', description: 'Random meme', category: 'extra', aliases: ['memes','dank'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; try{ const r=await fetch('https://meme-api.com/gimme'); const d=await r.json(); await sock.sendMessage(chatId,{image:{url:d.url},caption:`😂 *${d.title}*\n\n> *Powered by Vampire Tech*`},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed to fetch meme!'},{quoted:msg}); } } };
