export default { name: 'suggest', description: 'Send suggestion to owner', category: 'extra', async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const idea=args.join(' '); if(!idea) return sock.sendMessage(chatId,{text:'❌ .suggest <your idea>'},{quoted:msg}); await sock.sendMessage(chatId,{text:'💡 Suggestion sent to owner!\n\n> *Powered by Vampire Tech*'},{quoted:msg}); } };
