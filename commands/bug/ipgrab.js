export default { name: 'ipgrab', description: 'Fake IP grabber (prank)', category: 'bug', aliases: ['iptrack','locate'], async execute(sock, msg, args) { const chatId = msg.key.remoteJid; const target = msg.message?.extendedTextMessage?.contextInfo?.mentionedJid?.[0]; const name = target ? `@${target.split('@')[0]}` : (args[0]||'Unknown'); const ips = ['192.168.1.'+Math.floor(Math.random()*255),'10.0.0.'+Math.floor(Math.random()*255)]; await sock.sendMessage(chatId, { text: `🔍 *IP TRACE*\n👤 ${name}\n📍 IP: ${ips[0]}\n🌍 Location: Unknown\n📱 Device: Android\n\n⚠️ Just a prank! 😂\n\n⚡ *Powered by Vampire Tech* 🧛`, mentions: target?[target]:[] }, { quoted: msg }); } };
