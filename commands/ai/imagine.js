export default { name:'imagine', description:'Generate AI image', category:'ai', aliases:['draw','generate','img'], async execute(sock,msg,args){ const c=msg.key.remoteJid; const p=args.join(' '); if(!p) return sock.sendMessage(c,{text:'❌ .imagine <description>'},{quoted:msg}); await sock.sendMessage(c,{image:{url:`https://image.pollinations.ai/prompt/${encodeURIComponent(p)}`},caption:`🎨 ${p}\n\n> *Vampire Tech*`},{quoted:msg}); } };
