export default { name:'gpt4', description:'Chat with GPT-4', category:'ai', aliases:['chatgpt4'], async execute(sock,msg,args){ const c=msg.key.remoteJid; const q=args.join(' '); if(!q) return sock.sendMessage(c,{text:'❌ .gpt4 <question>'},{quoted:msg}); try{ const r=await fetch(`https://apis.xwolf.space/api/ai/gpt?q=${encodeURIComponent(q)}`); const d=await r.json(); await sock.sendMessage(c,{text:`🧠 *GPT-4*\n\n${d.result}\n\n> *Vampire Tech*`},{quoted:msg}); }catch(e){ await sock.sendMessage(c,{text:'❌ Failed!'},{quoted:msg}); } } };
