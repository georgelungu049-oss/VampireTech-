export default { name: 'groq', description: 'Chat with Groq AI', category: 'ai', aliases: ['ai','ask'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const q=args.join(' '); if(!q) return sock.sendMessage(chatId,{text:'❌ .groq <question>'},{quoted:msg}); try{ const r=await fetch(`https://apis.xwolf.space/api/ai/groq?q=${encodeURIComponent(q)}`); const d=await r.json(); await sock.sendMessage(chatId,{text:`🤖 *Groq AI*\n\n${d.result}\n\n⚡ *Vampire Tech* 🧛`},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
