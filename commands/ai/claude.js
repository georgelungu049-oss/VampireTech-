export default { name: 'claude', description: 'Chat with Claude AI', category: 'ai', aliases: ['claudeai','anthropic'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const q=args.join(' '); if(!q) return sock.sendMessage(chatId,{text:'❌ .claude <question>'},{quoted:msg}); try{ const r=await fetch(`https://apis.xwolf.space/api/ai/claude?q=${encodeURIComponent(q)}`); const d=await r.json(); await sock.sendMessage(chatId,{text:`🤖 *Claude AI*\n\n${d.result}\n\n⚡ *Vampire Tech* 🧛`},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
