export default { name: 'gpt', description: 'Chat with GPT AI', category: 'ai', aliases: ['chatgpt','openai'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const q=args.join(' '); if(!q) return sock.sendMessage(chatId,{text:'❌ .gpt <question>'},{quoted:msg}); try{ const r=await fetch(`https://apis.xwolf.space/api/ai/gpt?q=${encodeURIComponent(q)}`); const d=await r.json(); await sock.sendMessage(chatId,{text:`🧠 *GPT AI*\n\n${d.result}\n\n⚡ *Vampire Tech* 🧛`},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
