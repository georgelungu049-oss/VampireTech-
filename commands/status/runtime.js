export default { name: 'runtime', description: 'Bot runtime details', category: 'status', aliases: ['running','alive'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const uptime=process.uptime(); const d=Math.floor(uptime/86400); const h=Math.floor((uptime%86400)/3600); const m=Math.floor((uptime%3600)/60); const s=Math.floor(uptime%60); const start=new Date(Date.now()-uptime*1000); await sock.sendMessage(chatId,{text:`⏰ *Runtime*\n\n🕐 Started: ${start.toLocaleString()}\n📅 Days: ${d}\n⏱️ Hours: ${h}\n⏰ Minutes: ${m}\n⏲️ Seconds: ${s}\n\n⚡ Total: ${d}d ${h}h ${m}m ${s}s\n\n> *Powered by Vampire Tech*`},{quoted:msg}); } };
