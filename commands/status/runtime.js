export default { name:'runtime', category:'status', aliases:['uptime','alive'], async execute(sock,msg){ const c=msg.key.remoteJid; const up=process.uptime(); const d=Math.floor(up/86400); const h=Math.floor((up%86400)/3600); const m=Math.floor((up%3600)/60); const s=Math.floor(up%60); await sock.sendMessage(c,{text:`⏰ *RUNTIME*\n\n${d}d ${h}h ${m}m ${s}s\n📅 Started: ${new Date(Date.now()-up*1000).toLocaleString()}\n\n> *Vampire Tech* 🧛`},{quoted:msg}); } };
