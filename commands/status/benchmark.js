export default { name: 'benchmark', description: 'Run speed benchmark', category: 'status', aliases: ['speedtest','bench'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const sent=await sock.sendMessage(chatId,{text:'⚡ *Benchmarking...*'}); const start=Date.now(); let test=0; for(let i=0;i<1000000;i++){test+=Math.sqrt(i);} const end=Date.now(); await sock.sendMessage(chatId,{text:`⚡ *Benchmark Results*\n\n🧮 1M calculations: ${end-start}ms\n📡 Bot latency: ${end-start}ms\n💪 CPU Score: ${Math.floor(1000000/(end-start))}\n\n> *Powered by Vampire Tech*`,edit:sent.key}); } };
