import fs from 'fs';
export default { name: 'dbstats', description: 'Database & storage stats', category: 'status', aliases: ['storage','disk'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; try{ const files=fs.readdirSync('./data'); let size=0; files.forEach(f=>{ try{ size+=fs.statSync('./data/'+f).size; }catch(e){} }); await sock.sendMessage(chatId,{text:`💾 *Storage Stats*\n\n📁 Data files: ${files.length}\n📊 Total size: ${(size/1024).toFixed(2)} KB\n🗄️ Session: ${fs.existsSync('./session')?'Active':'None'}\n📋 Commands: ${fs.readdirSync('./commands').length} folders\n\n> *Powered by Vampire Tech*`},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
