export default { name: 'feed', description: 'Feed on someone', category: 'vampire', aliases: ['drink','consume'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const mentioned=msg.message?.extendedTextMessage?.contextInfo?.mentionedJid?.[0]; const name=mentioned?`@${mentioned.split('@')[0]}`:(args[0]||'a human'); await sock.sendMessage(chatId,{text:`🩸 *FEEDING...*\nYou fed on ${name}!\n🍷 Delicious!\n\n> *Powered by Vampire Tech*`,mentions:mentioned?[mentioned]:[]},{quoted:msg}); } };
