export default { name: 'age', description: 'Calculate age from birth year', category: 'converter', aliases: ['birth','born'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const year=parseInt(args[0]); if(!year) return sock.sendMessage(chatId,{text:'❌ .age <birth-year>\nExample: .age 2000'},{quoted:msg}); const age=new Date().getFullYear()-year; await sock.sendMessage(chatId,{text:`🎂 *Age Calculator*\nBorn: ${year}\nAge: ${age} years old\n\n> *Powered by Vampire Tech*`},{quoted:msg}); } };
