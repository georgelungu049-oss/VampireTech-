export default { name: 'currency', description: 'Convert currency', category: 'converter', aliases: ['convert','exchange'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; if(!args[1]) return sock.sendMessage(chatId,{text:'❌ .currency <amount> <from> <to>\nExample: .currency 100 USD ZAR'},{quoted:msg}); const amount=args[0]; const from=args[1].toUpperCase(); const to=(args[2]||'ZAR').toUpperCase(); await sock.sendMessage(chatId,{text:`💱 *Currency*\n${amount} ${from} → ${to}\n\n🔗 https://www.google.com/search?q=${amount}+${from}+to+${to}\n\n> *Powered by Vampire Tech*`},{quoted:msg}); } };
