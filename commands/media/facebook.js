export default { name: 'facebook', description: 'Download Facebook videos', category: 'media', aliases: ['fb','fbdl'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const url=args[0]; if(!url) return sock.sendMessage(chatId,{text:'❌ .fb <facebook-url>'},{quoted:msg}); try{ const r=await fetch(`https://apis.xwolf.space/api/download/facebook?url=${encodeURIComponent(url)}`); const d=await r.json(); await sock.sendMessage(chatId,{text:`📥 *Facebook DL*\n✅ Downloaded!\n\n⚡ *Vampire Tech* 🧛`},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
