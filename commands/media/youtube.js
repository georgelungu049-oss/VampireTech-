export default { name: 'youtube', description: 'Download YouTube videos', category: 'media', aliases: ['ytdl','yt'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const url=args[0]; if(!url) return sock.sendMessage(chatId,{text:'❌ .youtube <url>'},{quoted:msg}); try{ const r=await fetch(`https://apis.xwolf.space/download/video?url=${encodeURIComponent(url)}&q=video`); const d=await r.json(); await sock.sendMessage(chatId,{text:`📥 *YouTube DL*\n✅ Downloading!\n\n⚡ *Vampire Tech* 🧛`},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
