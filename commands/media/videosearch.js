export default { name: 'videosearch', description: 'Search videos online', category: 'media', aliases: ['video','vsearch'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const q=args.join(' '); if(!q) return sock.sendMessage(chatId,{text:'❌ .videosearch <query>'},{quoted:msg}); try{ const r=await fetch(`https://apis.xwolf.space/api/search/videos?q=${encodeURIComponent(q)}&page=0`); const d=await r.json(); const v=(d.result||[]).slice(0,3).map((v,i)=>`${i+1}. ${v.title}`).join('\n'); await sock.sendMessage(chatId,{text:`🎬 *Video: ${q}*\n${v}\n\n⚡ *Vampire Tech* 🧛`},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
