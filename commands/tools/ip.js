export default { name:'ip', description:'IP information lookup', category:'tools', aliases:['iplookup','iptrace'], async execute(sock,msg,args){ const c=msg.key.remoteJid; const ip=args[0]||'8.8.8.8'; try{ const r=await fetch(`http://ip-api.com/json/${ip}`); const d=await r.json(); await sock.sendMessage(c,{text:`🌐 *IP: ${d.query}*\n📍 ${d.city}, ${d.country}\n🏢 ${d.isp}\n\n> *Vampire Tech*`},{quoted:msg}); }catch(e){ await sock.sendMessage(c,{text:'❌ Failed!'},{quoted:msg}); } } };
