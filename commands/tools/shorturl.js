export default { name: 'shorturl', description: 'Shorten a URL', category: 'tools', aliases: ['shorten','tinyurl'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const url=args[0]; if(!url) return sock.sendMessage(chatId,{text:'❌ .shorturl <url>'},{quoted:msg}); try{ const r=await fetch(`https://tinyurl.com/api-create.php?url=${encodeURIComponent(url)}`); const short=await r.text(); await sock.sendMessage(chatId,{text:`🔗 *Shortened!*\n${short}\n\n> *Powered by Vampire Tech*`},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
