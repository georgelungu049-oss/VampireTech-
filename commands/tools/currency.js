export default { name:'currency', description:'Currency converter', category:'tools', aliases:['exchange'], async execute(sock,msg,args){ const c=msg.key.remoteJid; if(args.length<2) return sock.sendMessage(c,{text:'❌ .currency <amount> <from> <to>\nExample: .currency 100 USD ZAR'},{quoted:msg}); await sock.sendMessage(c,{text:`💱 ${args[0]} ${args[1].toUpperCase()} → ${args[2]?.toUpperCase()||'ZAR'}\n🔗 https://google.com/search?q=${args[0]}+${args[1]}+to+${args[2]||'ZAR'}\n\n> *Vampire Tech*`},{quoted:msg}); } };
