export default { name: 'uppercase', description: 'Convert to UPPERCASE', category: 'tools', aliases: ['upper'], async execute(sock, msg, args) { const chatId = msg.key.remoteJid; const text = args.join(' '); if (!text) return sock.sendMessage(chatId, { text: '❌ Provide text!' }, { quoted: msg }); await sock.sendMessage(chatId, { text: `🔠 *UPPERCASE:*\n${text.toUpperCase()}\n\n> *Powered by Vampire Tech*` }, { quoted: msg }); } };
