export default { name: 'randomnum', description: 'Generate random number', category: 'tools', aliases: ['rand'], async execute(sock, msg, args) { const chatId = msg.key.remoteJid; const min = parseInt(args[0]) || 1; const max = parseInt(args[1]) || 100; const num = Math.floor(Math.random() * (max-min+1)) + min; await sock.sendMessage(chatId, { text: `🎲 *Random (${min}-${max}):* ${num}\n\n⚡ *Powered by Vampire Tech* 🧛` }, { quoted: msg }); } };
