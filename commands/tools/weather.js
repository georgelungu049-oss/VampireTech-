export default { name: 'weather', description: 'Get weather by city', category: 'tools', aliases: ['temp','climate'], async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const city=args.join(' ')||'Nairobi'; try{ const r=await fetch(`https://apis.xwolf.space/api/tools/weather?city=${encodeURIComponent(city)}`); const d=await r.json(); await sock.sendMessage(chatId,{text:`🌤️ *Weather: ${city}*\n\n${JSON.stringify(d.result||d).replace(/[{}"]/g,'').replace(/,/g,'\n')}\n\n⚡ *Vampire Tech* 🧛`},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
