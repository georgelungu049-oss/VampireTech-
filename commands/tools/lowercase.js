export default { name: 'lowercase', description: 'Convert to lowercase', category: 'tools', aliases: ['lower'], async execute(sock, msg, args) { const chatId = msg.key.remoteJid; const text = args.join(' '); if (!text) return sock.sendMessage(chatId, { text: '❌ Provide text!' }, { quoted: msg }); await sock.sendMessage(chatId, { text: `🔡 *lowercase:*\n${text.toLowerCase()}\n\n> *Powered by Vampire Tech*` }, { quoted: msg }); } };
