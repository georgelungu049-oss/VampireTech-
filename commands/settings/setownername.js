export default { name: 'setownername', description: 'Change owner name', category: 'settings', ownerOnly: true, async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const n=args.join(' '); if(!n) return sock.sendMessage(chatId,{text:'.setownername <name>\nCurrent: Paxton\n\n> *Powered by Vampire Tech*'},{quoted:msg}); await sock.sendMessage(chatId,{text:`✅ Owner: *${n}*\n\n> *Powered by Vampire Tech*`},{quoted:msg}); } };
