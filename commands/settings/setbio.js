export default { name:'setbio', category:'settings', aliases:['bio','status'], ownerOnly:true, async execute(sock,msg,args){ const c=msg.key.remoteJid; const b=args.join(' '); if(!b) return sock.sendMessage(c,{text:'.setbio <text>\n\n> *Vampire Tech* 🧛'},{quoted:msg}); try{ await sock.updateProfileStatus(b); await sock.sendMessage(c,{text:'🦇 Bio updated!\n\n> *Vampire Tech* 🧛'},{quoted:msg}); }catch(e){ await sock.sendMessage(c,{text:'💀 Failed!'},{quoted:msg}); } } };
