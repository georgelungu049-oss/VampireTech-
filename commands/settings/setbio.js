export default { name: 'setbio', description: 'Change WhatsApp bio', category: 'settings', ownerOnly: true, async execute(sock, msg, args) { const chatId=msg.key.remoteJid; const b=args.join(' '); if(!b) return sock.sendMessage(chatId,{text:'.setbio <text>\n\n> *Powered by Vampire Tech*'},{quoted:msg}); try{ await sock.updateProfileStatus(b); await sock.sendMessage(chatId,{text:'✅ Bio updated!\n\n> *Powered by Vampire Tech*'},{quoted:msg}); }catch(e){ await sock.sendMessage(chatId,{text:'❌ Failed!'},{quoted:msg}); } } };
